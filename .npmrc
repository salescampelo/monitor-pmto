# legacy-peer-deps removed 2026-04-27.
# Original band-aid was added in commit b6e59bb to work around the vite 8 vs
# vite-plugin-pwa@1.2.0 peer-dep conflict. Replaced by pinning vite back to ^7
# (see package.json), which vite-plugin-pwa officially supports.
# Re-evaluate when vite-plugin-pwa publishes a release with vite ^8 in its peer range.
